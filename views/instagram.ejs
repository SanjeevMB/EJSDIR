<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
</head>
<body>
    <h3>Welcome to the @<%= userName.name %> Instagram</h3>
    <button>Follower : <%= userName.followers %></button>
    <button>Followings : <%= userName.following %></button>
    <hr>
    <% for (let i = 0; i < userName.posts.length; i++)  { %>
        <img src="<%= userName.posts[i].image %>" alt="post" width="300px" height="300px">
        <br>
        <span>Likes: <%= userName.posts[i].likes %></span>
        <span>Likes: <%= userName.posts[i].comments %></span>
        <br>          
   <% } %>
</body>
</html>