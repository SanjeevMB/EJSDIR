<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
</head>
<body>
    <h3>Welcome to the @<%= user.name %> Instagram</h3>
    <button>Follower : <%= user.followers %></button>
    <button>Followings : <%= user.following %></button>
    <hr>
    <% for (let i = 0; i < user.posts.length; i++)  { %>
        <img src="<%= user.posts[i].image %>" alt="post" width="300px" height="300px">
        <br>
        <span>Likes: <%= user.posts[i].likes %></span>
        <span>Likes: <%= user.posts[i].comments %></span>
        <br>          
   <% } %>
</body>
</html>